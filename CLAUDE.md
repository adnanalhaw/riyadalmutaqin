# رياض المتقين — دليل السياق (يُقرأ أولاً في كل جلسة)

> **⛔ قبل أي عمل: اقرأ هذا الملف كاملاً + `docs/DECISIONS.md`.**
> هذا الملف يُقرأ تلقائياً عند فتح Claude Code في هذا المجلد — وهو الذي يمنع
> تشتّت المشروع بين المحادثات. **كل جلسة تنتهي بتحديثه** (قسم «الحالة» و«المتبقي»).

## ما هو المشروع

**رياض المتقين (riyadalmutaqin.com)** — منصّة دعوية لقناة «القرآن والسنّة بفهم سلف
الأمّة»، وقاعدةٌ لبناء **بيانات شرعية موثّقة تُدرَّب عليها نماذج الذكاء الاصطناعي**
لتجيب عن الأسئلة الدينية بدقّة وإسناد.

**⚠️ لا علاقة له بمشروع رِزق** (مستودع `Adnan-erp-2` · Render · riziqapp.com):
مشروعان منفصلان كلياً في الكود والنشر. الصلة الوحيدة **تجارية**: الكوربوس المعتمد
هنا يكون في المنصّتين — رِزق **يبيعه** B2B، ورياض المتقين **يدرّب به**
(التفصيل في `docs/DECISIONS.md`).

## الحزمة التقنية

- **الخادم:** Cloudflare Workers · TypeScript · `src/index.ts` (موجّه `/api` واحد).
- **القاعدة:** Cloudflare D1 (SQLite) — الترحيلات في `migrations/` (17 ترحيلة).
- **الملفات:** Cloudflare R2 (صوت/صور/PDF لاحقاً).
- **الواجهة:** HTML/CSS/JS فانيلا عربي RTL في `public/` (أصول ثابتة، بلا أطر ولا build).
- **النشر:** كل دفعة إلى `main` ← ترحيلات D1 ← نشر تلقائي (`.github/workflows/deploy.yml`).

## أوامر أساسية

```bash
npm install
npm run typecheck          # فحص الأنواع — شغّله قبل كل commit
npm run dev                # خادم محلي (wrangler)
npm run db:migrate:local   # ترحيلات محلية
```

## ⚠️ قواعد العمل (لا تتجاوزها)

1. **مستودع واحد لمشروع واحد.** لا تخلط كود رِزق هنا ولا العكس. المستودع القديم
   للمشروع (فرع داخل `Adnan-erp-2`) **مجمَّد ومهجور** — لا تعمل عليه.
2. **جلسة واحدة تكتب كوداً في وقت واحد.** وقبل أي عمل: `git pull`.
3. **الأسرار لا تدخل git إطلاقاً** — أسرار GitHub (Settings → Secrets → Actions)
   والنشر يثبّتها في Cloudflare. لا تطلب كلمات مرور المستخدم أبداً.
4. **الصدق في الأرقام والحالة** — لا وعود، ولا «تم» قبل التحقق الفعلي.
5. **لا جواب شرعي بلا مصدر** في كوربوس التدريب — مفروض برمجياً في مساري المساهمة
   والاعتماد معاً.
6. **RTL:** النصوص عربية، وكل كلمة إنجليزية بين قوسين على سطر منفصل في الردود.

## الحالة الآن (2026-09-10 — بوصلة قبلة + مبدّل اللغة + خريطة المساجد)

**✅ مبنيّ ومنشور ويعمل:**
- **الأدوار الأربعة:** متعلّم · معلّم · مدير الموقع (manager) · مدير النظام (admin)
 — جلسات كوكي HttpOnly + PBKDF2 + حدّ محاولات.
  صفحة الدخول (`/login`) تعرض التبويبات الأربعة (تسمية الأدمن في `/admin`: «أدمن»)
  مع `?role=`؛ التوجيه بعد الدخول حسب `user.role` من الواجهة البرمجية لا التبويب.
- **الواجهة العامة:** الرئيسية · عن الموقع · الدروس المباشرة · مكتبة المقاطع ·
  المعلّمون · بطاقة المشاركة (+ **رمز QR**).
- **أدوات المسلم:** مواقيت الصلاة · **قبلة** (`/qibla`: وردة ش/ق/ج/غ + علامات
  درجات + سهم ذهبي وكعبة واضحة؛ الوردة تدور مع اتجاه الجهاز والسهم نحو الكعبة ·
  خريطة صغيرة اختياريّة بخط ذهبي نحو الكعبة) · الأذكار · التسبيح · الختمة ·
  التقويم الهجري · خريطة المساجد (**خريطة Leaflet + OSM** مع موقع المستخدم
  وعلامات المساجد من Overpass، والقائمة كما هي) · القرآن (رسم عثماني + تلاوة).
- **🌐 اللغة:** زر الكرة في الترويسة يحفظ الاختيار (`rm_lang` + `googtrans`
  + هاش `#googtrans`) ويحمّل Google Translate لكل الصفحات. العربية تبقى الأصل
  (مسح الكوكي وإعادة التحميل). شريط Google العلوي والبلونات مخفيّة
  (`.goog-te-banner-frame` + `.VIpgJd-ZVi9od-ORHb*` + `#goog-gt-tt`) دون إخفاء
  جذر `.skiptranslate` حتى لا يتوقف المحرّك. حدود GT موثّقة في README.
- **🧮 حاسبة الزكاة (5 أنواع):** النقد (دفتر دخل/مصروف بتتبّع الحَوْل) · الذهب ·
  الفضّة · الأسهم · عروض التجارة — بأسعار سوق **حيّة** (`/api/gold-price`).
- **🤖 ساهم بتدريب الذكاء (`/train`):** مساهمة تطوعية شفّافة (سؤال+جواب موثّق /
  سؤال) لكل الأدوار **عدا admin** · إقرار شفافية إلزامي · سقف يومي ذرّي 30 ·
  صفحة **مراجعة المدير** (`/manager/training`) بالاعتماد والتصحيح والردّ ·
  **تصدير JSONL مجهّل الهوية**.
- **المعلّم:** الدروس (+رفع يوتيوب) · المقاطع والصوتيات · المونتاج · التحليلات ·
  النشر على القنوات (تيليجرام مباشر + **Webhook توزيع** لبقيّة المنصّات).
  استوديو `/teacher/editor`: معاينة ريلز **9:16** قابلة للتشغيل على الجوال
  (H.264 Baseline + AAC + 25fps + `playsinline`) وتركيز فيسبوك/انستقرام ريلز.
- **المدير:** لوحة القيادة · طلبات التعليم · اعتماد المحتوى · مراجعة التدريب · التحليلات
 · **🔗 ربط حسابات النشر** (`/manager/connections`) · وأدوات المعلّم كاملةً (مونتاج/نشر).
  بطاقات بارزة في `/manager` و`/admin` لاستوديو المونتاج والنشر الرسمي؛ شريط الأدمن
  يربط `/teacher/editor` و`/teacher/publish` و`/manager/connections` مباشرةً.
  صفحات الاستوديو/النشر تبدّل الشريط إلى لوحة المدير/الأدمن وتعرض «عودة للوحة…».
- **📣 النشر التلقائي:** نقطة تسليم موحّدة (`deliverPost`) تخدم «النشر الآن» والمجدول
  وموافقة المدير: تيليجرام مباشرةً · **فيسبوك وانستقرام عبر Meta Graph** —
  **المدير/الأدمن → حساب الموقع الرسمي** (`getSiteAccount`، صفحة رياض المتقين
  و`@almutaqyn`) · **المعلّم → صفحته الشخصية فقط** (`getAccount(authorId)` بعد
  موافقة المدير؛ يُرفض إن طابق `page_id` الرسمية) · **يوتيوب برفع فعليّ** للفيديو ·
  وما بقي عبر Webhook التوزيع. **فيديو فيسبوك العمودي → Page Reels API**
  (`video_reels` start → rupload → finish/PUBLISHED) حتى يظهر في تبويب الريلز؛
  بايتات الريل تُقرأ من `env.MEDIA` إن كان الرابط `/api/media/<key>` (تجنّب
  HTTP 522 عند fetch النطاق العام من داخل الـ Worker) وإلا جلب خارجي.
  الأفقي 16:9 يبقى على `/{page}/videos`؛ وإن فشل الريل يُرفع احتياطياً كفيديو
  صفحة. فيديو انستقرام: حاوية `REELS` ثم انتظار `FINISHED` ثم `media_publish`؛
  إن لم يجهز يُعلَّم `ig_creation_id` و`scheduled_at=now` إن كان فارغاً ويكمله
  cron حتى لو `scheduled_at` كان null (عطل منشور 13: الكرون كان يشترط الموعد).
  المعلّم يربط صفحته من `/teacher/publish`.
- **🔐 Facebook Login for Business:** إن وُجد سرّ `FB_LOGIN_CONFIG_ID` يُمرَّر
  `config_id` في حوار OAuth **بدل** `scope` (الكلاسيكي يبقى إن غاب السرّ).
  رسالة فشل نشر انستقرام تفرّق: بلا FLB → اضبط السرّ وأعد الربط · مع FLB أو
  Meta `#10` → صلاحية التطبيق (`instagram_content_publish`) لا إعادة الربط.

- **التذكيرات:** الصلاة على النبي ﷺ (كل ٥د) · صيام التطوّع (الإثنين/الخميس/البيض) ·
  **وِرد التسبيح** (كل ٢٠د).
- **البنية:** ترويسات أمان + SEO + sitemap كاملة + مهارات Cloudflare للوكلاء
  (`.claude/skills/`).

**🔑 مفاتيح مضبوطة:** `CLOUDFLARE_API_TOKEN` · `CLOUDFLARE_ACCOUNT_ID` (النشر نجح بهما).

## المتبقي (بالأولوية — حدِّثه في نهاية كل جلسة)

1. **`OWNER_EMAIL`** — سرّ ناقص. بدونه لا يترقّى المالك لمدير نظام عند الدخول
   (خطوة النشر تُتخطّى بصمت). أضِفه ← أعِد النشر ← سجّل حساباً بنفس البريد.
2. **حذف Workers القديمة** في حساب Cloudflare (`riyadalmutaqin` بلا شرطة ·
   `odd-pond-a24c`) بعد التحقق البصري من الموقع. الحيّ هو `riyad-almutaqin`.
   الجرد الكامل يثبت أن الحذف آمن — `docs/DECISIONS.md` §3-ب.
3. **📚 كتب PDF (مقرَّر ولم يُبنَ):** رفع المعلّم ← اعتماد المدير · رفع المدير ←
   معتمد تلقائياً. الجدول جاهز (`ai_documents`) وR2 مربوط — ينقص المسارات والواجهة.
4. **جسر البيانات إلى رِزق** — يدويّ الآن (زر تصدير JSONL). الأتمتة قرار لاحق.
5. **نقل استوديو التصاميم** من النسخة القديمة (٥ تصاميم + ٣ قوالب فيديو).
6. **مفاتيح اختيارية:** `FB_APP_ID/FB_APP_SECRET` (ربط فيسبوك وانستقرام —
   الدليل في README) · `GOOGLE_CLIENT_ID/SECRET` (رفع يوتيوب) ·
   `TELEGRAM_BOT_TOKEN/CHAT_ID` (نشر) · `PUBLISH_WEBHOOK_URL` (توزيع المنصّات) ·
   `RESEND_API_KEY` (بريد استعادة كلمة المرور). كلها يثبّتها النشر الآلي من أسرار
   GitHub، وغيابها لا يكسر شيئاً — تظهر القناة «غير مُفعَّلة» بصدق.
   **صلاحيات Meta الحالية:** `business_management` · `pages_show_list` ·
   `pages_manage_posts` · `pages_read_engagement`. لا تُطلب صلاحيات انستقرام
   في OAuth (`instagram_business_*` يرفضها Meta بـ Invalid Scopes) — الاكتشاف
   عبر حقول Graph: `instagram_business_account` ثم `connected_instagram_account`
   ثم `instagram_accounts` ثم أصول الأعمال إن وُجد `business_management`.
   `POST /api/connections/meta/refresh-instagram` يعيد الاكتشاف دون OAuth
   (المسار الأساسي)، ويميّز ربط مركز الحسابات عن حساب أعمال جاهز للواجهة.
   إن فرغ Graph وبقي معرّف يدوي محفوظاً: **لا يُمسَح** — الردّ `kept_manual`
   و`instagram_linked: true`. المسح فقط عند فكّ ربط فيسبوك.
   احتياطي المدير: `POST /api/connections/meta/set-instagram` يلصق
   `ig_user_id` (أرقام) و`ig_username` اختيارياً إن ظهر الحساب في Meta UI
   وبقيت حقول Graph فارغة — يظهر `instagram_linked`. نشر Graph يحتاج صلاحية
   محتوى انستقرام؛ لا تُعاد إلى OAuth الكلاسيكي (Invalid Scopes).
   **Login for Business مُنفَّذ في الكود:** أضِف سرّ GitHub `FB_LOGIN_CONFIG_ID`
   (من Meta App Dashboard → Facebook Login for Business → Configurations)
   ثم انشر وأعد ربط فيسبوك من `/manager/connections` (فصل ثم إعادة) — نفس
   الحوار للمعلّم على صفحته من `/teacher/publish`.
   التوكن الرسمي لمدير الموقع/الأدمن فقط؛ المعلّم يربط صفحته الخاصة ويُمنَع من
   الصفحة الرسمية.
   **بعد إعادة الربط:** إن نجح فيسبوك وبقي انستقرام يرفض بـ `(#10) Application
   does not have permission` فالخلل في صلاحية التطبيق عند Meta
   (`instagram_content_publish` ناقصة/غير معتمدة/جاهزة للاختبار فقط) — الرسالة
   للمستخدم لا تطلب إعادة الربط مجدداً (`IG_APP_PERMISSION_PUBLISH_ERROR`).
7. **البث المباشر (المرحلة ٧)** — لم يُبنَ.

## الوثائق المرجعية

- **`docs/DECISIONS.md`** — قرارات المالك المُلزمة (العلاقة مع رِزق · قواعد الكتب ·
  سياسة موقع واحد · جرد النسخ القديمة). **مُلزم لأي عمل جديد.**
- **`README.md`** — البنية والمراحل وخطوات الإعداد على Cloudflare.

## أسلوب التعامل مع المشغّل

مهمة واحدة كل رسالة بخطوات نقر دقيقة (يعمل من الهاتف غالباً) · لا تفترض أنه نفّذ
خطوة — تحقّق · صدق كامل بالحالة والأرقام.
